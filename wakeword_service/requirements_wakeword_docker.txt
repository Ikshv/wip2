# Wyoming + YAML
wyoming>=1.0.0
zeroconf>=0.131.0
ifaddr>=0.1.7
numpy>=1.20.0
PyYAML>=6.0

# openWakeWord ONNX path uses bundled melspectrogram.onnx + embedding_model.onnx (no tflite).
# The openwakeword package is installed in Dockerfile with --no-deps to skip the Linux
# tflite-runtime requirement that breaks many arm64 builds.
onnxruntime>=1.10,<2
tqdm>=4,<5
scipy>=1.3,<2
scikit-learn>=1,<2
requests>=2,<3

# Optional fallback if openWakeWord fails to initialize
faster-whisper>=1.0.0
